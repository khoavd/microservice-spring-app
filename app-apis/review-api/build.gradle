plugins {
    id 'org.openapi.generator' version '7.5.0'
}

group = 'com.dogoo.ecommerce.revire.api'
version = '1.0-SNAPSHOT'

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-webflux'
    implementation group: 'org.openapitools', name: 'jackson-databind-nullable', version: '0.2.6'
    implementation("jakarta.validation:jakarta.validation-api:3.0.2")
}

openApiGenerate {
    generatorName.set("spring")
    inputSpec.set("$rootDir/specs/review-rest-openapi.yaml")
    apiPackage.set("com.dogoo.ecommerce.revirew.api")
    modelPackage.set("com.dogoo.ecommerce.review.api.model")
    configOptions.put("gradleBuildFile", "false")
    configOptions.put("useSpringBoot3", "true")
    configOptions.put("documentationProvider", "none")
    configOptions.put("datesLibrary", "java8")
    configOptions.put("reactive", "true")

    generateApiTests.set(false)
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}

sourceSets.getByName("main").java.srcDir("$buildDir/generate-resources/main/src/main/java")

tasks.compileJava.dependsOn(tasks.openApiGenerate)